# 开发日志

记录 clinicaltrials-intel 技能包的开发过程、版本演进和重要决策。

---

## v3.1.0 (2026-07-18) — 可复用靶点 YAML + 单一靶点推送 + 日报关键词

### 一、胰腺癌靶点 YAML 化（可复用）

**动机**：靶点关键词长期散落在 `.env KEYWORDS` 长串里，难维护、难跨项目复用。

**方案**：
- 新增 `assets/pancreatic_targets.yaml`（core / A / B / C 分组 + aliases + keywords）
- 新增 `lib/targets.py`：`expand_keywords` / `resolve_default_keywords` / `match_target` / `resolve_target_query`
- `.env` 默认 `KEYWORDS=yaml`；也可逗号列表临时覆盖

覆盖清单：EGFR、MET、HER2、TROP2、CLDN18.2、TF、MSLN、B7-H3(CD276)、Nectin-4、NTRK、CDH17、CEACAM5、MTAP、MUC1、FOLR1、DLL3、CA125 等。

### 二、单一靶点 → GeWe 文字

- 主菜单新增 **5️⃣ 单一靶点推送**（默认中国中心 + top10 + gewe_txt）
- CLI：`python3 main.py --target B7H3 --china --top 10 --send-gewe-txt`
- 输入与 YAML 做泛化匹配：大小写、分隔符、别名（`b7h3`/`CD276`/`claudin18.2`）
- 未命中 YAML 不阻断，按原文检索（菜单二次确认）
- 仍复用 `run_cli_mode` 两阶段：抓取 → 翻译落地 → 渠道推送

### 三、日报模版增加检索关键词

汇总文案由：

```text
发现 N 个符合条件的临床试验
```

升级为：

```text
发现 N 个符合条件的临床试验
检索关键词: B7-H3 (CD276)（B7H3 / B7-H3 / CD276）
```

实现于 `lib/content_builder._format_keywords_line`；`main.py` 与 `daily_ctgov_check_tgbot.py` 均写入。

### 四、验证

- YAML 展开 41 词；别名匹配用例通过
- 在线 CTGov：`B7H3` + china top3 可返回试验
- 未提交任何真实凭据；`.fastgpt.env.txt` 等本地杂项加入 gitignore

### 五、文档

- 更新 `README.md` / `SKILL.md` / `references/config-reference.md`
- 新增 `HANDOFF.md` 版本交接

---

## v3.0.0 (2026-06-21) — 多癌症/病种支持 + 品牌文案通用化

### 重大特性：从胰腺癌单病种升级为多病种通用平台

将技能包从「胰腺癌专用」升级为「多癌症/病种通用」平台，支持任意疾病
（各类癌症、罕见病等），无需为不同疾病维护多份代码。

### 一、多疾病支持（配置切换）

**设计决策**：采用配置切换方案（单仓库多疾病），而非为每种疾病单独建仓库。
疾病本质上只是一个查询参数，重复代码违背 DRY 原则。

**实现方式**：
- 疾病由 `.env` 的 `SEARCH_CONDITION` 或命令行 `--condition` 控制，一次运行一种疾病
- 落地目录、FastGPT 集合自动按疾病分：`output/{date}-{disease}/`

**Bug 修复**：
- `build_push_content` 新增 `condition` 参数，修复 `--condition` 不同步落地目录名的 bug

### 二、品牌文案通用化（新增 `lib/branding.py`）

新增 `lib/branding.py` 集中管理标题/footer，按疾病区分：

| 场景 | 标题 | footer |
|------|------|--------|
| 胰腺癌（保持原样） | 🏥 小胰宝临床情报小组日报 | 小胰宝社区志愿者...关注"小胰宝助手"公众号 |
| 其它癌症/病种（通用） | 🏥 小x宝{疾病中文}临床情报小组日报 | 小x宝社区志愿者...关注"小胰宝"公众号，github搜索opencare社区 |

含疾病英中映射表（胰腺癌/乳腺癌/肺癌/胃癌/结直肠癌/肝癌/前列腺癌/白血病/淋巴瘤），
未命中的罕见病等回退原文 condition。

### 三、真实测试验证

已用 3 种疾病端到端真实测试（抓取→翻译→推送到微信群）：
- ✅ 胰腺癌：文案保持原样不变
- ✅ 乳腺癌：目录 `Breast_Cancer`，标题「小x宝乳腺癌...」，footer 通用
- ✅ 胃癌：目录 `Gastric_Cancer`，标题「小x宝胃癌...」，footer 通用

### 四、开发约定

- 品牌文案统一从 `lib/branding.py` 的 `get_title()` / `get_footer()` 取，禁止硬编码
- 新增疾病中文映射加到 `branding.py` 的 `_DISEASE_CN` 字典

---

## v2.2.0 (2026-06-21) — 开箱可配置部署

### 技能包自带完整业务代码

技能包从「纯文档技能」升级为「自带完整业务代码的开箱可配置仓库」。

- 引入主项目完整业务代码（5 个入口脚本 + lib/ 全量模块），保持原目录结构
- 修正 `requirements.txt`，补齐主项目漏掉的 `PyYAML`（import yaml 致命缺口）
- 新增 `scripts/setup.sh`：一键部署（建 venv + 装依赖 + 生成配置 + 建运行时目录）
- 新增 `scripts/check_config.py`：配置校验（🔴依赖 / 🟡LLM / 🟢渠道分级报告）
- 新增 `docs/credentials-guide.md`：小白凭据申请指南（TG/飞书/GeWeChat/FastGPT/翻译模型）

### 文档完善

- 去 ZCode 化为通用技能包，不绑定特定产品
- 补充 GeWeChat 付费服务提示（官网 https://www.geweapi.com/#/newHome）
- 主菜单快捷推送：GeWe 文字为默认入口（选项3），卡片下移到选项4并加配置提示

---

*日志更新时间：2026-07-18*
