# ============================================================
# clinicaltrials-intel skill - Python 依赖
# ============================================================
# 安装(二选一):
#   uv pip install -r requirements.txt        # 推荐,快
#   pip install -r requirements.txt
#
# 说明:
#   - PyYAML:lib/config.py 与 lib/llm_client.py 均 import yaml,
#     主项目原 requirements.txt 漏装此项会导致启动直接崩溃,此处补齐。
#   - urllib3:telegram/gewe/ctgov_api 显式 import 以禁告警,显式声明。
#   - 其余(openai 的传递依赖 httpx/pydantic 等会自动安装,无需列出)。

requests>=2.32.0
python-dotenv>=1.0.0
openai>=1.0.0
apscheduler>=3.10.0
lark-oapi>=1.0.0
PyYAML>=6.0
urllib3>=2.0
# ChiCTR 模块 HTML 解析（lib/chictr，实验功能，默认不接入 main 流程）
beautifulsoup4>=4.12.0
